function moveTwoAheadII(num) {
    let start = 2; // Starting point
    while (start <= num) { // Loop until start is less than or equal to num
        console.log(start); // Print the current value of start
        start = start + 2; // Increment start by 2
    }
}

// Example Input and Output
moveTwoAheadII(12);


